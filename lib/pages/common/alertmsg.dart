import 'package:flutter/material.dart';

class AlertMsg extends StatelessWidget {
  const AlertMsg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}